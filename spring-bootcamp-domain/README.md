# Domain Module

Domain module with JPA entities, repositories and baseline configuration shared by the different 
chapters to reduce code duplication. This module does not provide any runnable integration tests
on its own.
